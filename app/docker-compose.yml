services:
  hr-api:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: secure-hr-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - JWT_SECRET=local-dev-secret-change-me-32-characters